---
title: "Assignment 8"
subtite: "Alex Reich's Homework"
output: html_notebook
---

Task 1: Heat Maps


```{r}
library(ggplot2)
library(wesanderson)
library(tvthemes)
library (tidyverse)
library (dplyr)
library (cowplot)
library (viridis)
NPGE_PDO.dat <- read.csv("/Users/alexandrareich/Desktop/DataVIzFish/Work_dir/npgo-pdo.csv")
```

Heat Map 1: Show NPGO by month and year
```{r}
ggplot(data=NPGE_PDO.dat)+aes(x= Year,  y= Mon, fill=NPGO)+ scale_fill_viridis()+theme_cowplot()+geom_tile()
```

Heat Map 2:
```{r}
ggplot(data=NPGE_PDO.dat)+aes(x= Year,  y= Mon, fill=PDO)+ scale_fill_viridis()+theme_classic() +geom_tile()
```

Heat Map 3
```{r}
#need a new variable
NPGO <- NPGE_PDO.dat$NPGO
PDO <- NPGE_PDO.dat$PDO

newV <- PDO/NPGO
newV2 <-NPGO/PDO

```
```{r}
ggplot(data=NPGE_PDO.dat)+aes(x= Year,  y= Mon, fill=newV)+ scale_fill_viridis()+theme_cowplot() +geom_tile()
```
Heat Map 3:
```{r}
ggplot(data=NPGE_PDO.dat)+aes(x= Year,  y= Mon, fill=newV2)+ scale_fill_viridis()+theme_cowplot() +geom_tile()
```
2)
Around 1977 and 1989 the NPDO spikes. After 1989 which the NPDO dips. The PDO starts to rise around 1977 and declines around 1989.


TASK 2: Plotting multiple proportions
```{r}
#faceting with color, maybe?
#load the data
NARW.data<- read.csv("/Users/alexandrareich/Desktop/DataVIzFish/Work_dir/NARW_mort.csv")
```

```{r}
#plot
ggplot(data=NARW.data) +aes (x=Year, y=Country, fill=Sex,)+ geom_tile()+facet_wrap(~Year) + 
```

```{r}
ggplot(data=NARW.data) +aes (x=CauseDeath, fill=Country, shape=Sex)+ geom_dotplot(binwidth = 1)+facet_wrap(~Year) + scale_fill_avatar(palette="AirNomads")+theme_classic()
```
nice try , but not perfect
```{r}
ggplot(data=NARW.data) +aes (x=CauseDeath, fill=Country)+ geom_bar(position=position_dodge())+facet_wrap(~Year) + scale_fill_avatar(palette="AirNomads")+theme_classic()
```
almost
```{r}
ggplot(data=NARW.data) +aes (x=CauseDeath, fill=Country)+ geom_bar(position=position_dodge())+facet_grid(vars(Sex),vars(Year)) + scale_fill_avatar(palette="AirNomads")+theme_classic()
```


